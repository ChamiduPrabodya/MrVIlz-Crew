{
  "name": "client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "powershell -NoProfile -Command \"$job = Start-Job -ScriptBlock { while ($true) { Start-Sleep -Milliseconds 500; try { Invoke-WebRequest -UseBasicParsing http://localhost:5173/ | Out-Null; Start-Process msedge 'http://localhost:5173/'; break } catch {} } }; vite\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "motion": "^12.39.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
